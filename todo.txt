add App, currently, daily, warning and warningPreview components
currently is shown as the largest on the page by default
daily is shown beside or below, when a day from daily is selected it is shown large
show components using fake json data
style components & add skycons
set state
add api calls(may have to limit calls?)
set api data to state
pass state as props to components
*done* date converter util
give options for date and time, date only, time only for date converter
tuncate/show warnings for main page
display full warnings on secondary page